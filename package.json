{
  "name": "mighty-gpio",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build:dev": "ncc build src/old.ts -o dist --source-map",
    "build:prod": "ncc build src/old.ts -m -o dist",
    "start:socket": "ts-node tests/socket.ts",
    "start:socket:debug": "node --inspect-brk --enable-source-maps -r ts-node/register tests/socket.ts",
    "start": "ts-node src/old.ts",
    "start:debug": "node --inspect-brk --enable-source-maps -r ts-node/register src/old.ts",
    "lint": "prettier --write {src,types}/*.ts"
  },
  "dependencies": {
    "@types/node": "^22.14.1",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "express": "^5.1.0",
    "prettier": "^3.5.3",
    "socket.io": "^4.8.1",
    "typescript": "^5.5.3"
  },
  "optionalDependencies": {
    "array-gpio": "^1.7.3"
  }
}
